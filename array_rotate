#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    int t,i;
    scanf("%d",&t);
    while(t--){
        int p;
        scanf("%d",&p);
        int n;
        scanf("%d",&n);
        int array[n];
        for(i=0;i<n;i++)
        {
            scanf("%d",&array[i]);
        
        }
        for(i=p;i<n;i++)
            printf("%d \n",array[i]);
        for(i=0;i<p;i++)
            printf("%d ",array[i]);
        
        #printf("\n");
        
    }
    return 0;
}
